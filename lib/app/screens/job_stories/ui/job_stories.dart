import 'package:flutter/material.dart';

class JobStories extends StatelessWidget {
  const JobStories({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Job Stories'),
    );
  }
}
