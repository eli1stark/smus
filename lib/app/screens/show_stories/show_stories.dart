import 'package:flutter/material.dart';

class ShowStoriesDependencies {
  
}

class ShowStories extends StatelessWidget {
  const ShowStories({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Show Screen'),
    );
  }
}
